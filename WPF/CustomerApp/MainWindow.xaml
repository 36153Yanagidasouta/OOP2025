<Window x:Class="CustomerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomerApp"
        mc:Ignorable="d"
        Title="顧客管理アプリケーション" Height="650" Width="600" Background="#778899">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="氏名:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="NameTextBox" Grid.Row="0" Grid.Column="1" Margin="0,5"/>

                <Label Content="電話番号:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="PhoneTextBox" Grid.Row="1" Grid.Column="1" Margin="0,5"/>

                <Label Content="住所:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="AddresTextBox" Grid.Row="2" Grid.Column="1" Margin="0,5"/>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">

                <Border BorderBrush="Black" BorderThickness="1" Width="302" Height="127" Margin="0,0,10,0">
                    <Image x:Name="CustomerImage" 
                           Stretch="Uniform" 
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
                </Border>

                <StackPanel Orientation="Vertical">
                    <Button Content="新規登録" x:Name="SaveButton" Click="SaveButton_Click" Width="200" Height="40" Margin="5" FontWeight="Bold" Background="#e6e6fa"/>
                    <Button Content="削除" x:Name="DeleteButton" Click="DeleteButton_Click" Width="200" Height="40" Margin="5" FontWeight="Bold" Background="#008b8b"/>
                    <Button Content="更新" x:Name="UpdateButton" Click="UpdateButton_Click" Width="200" Height="40" Margin="5" FontWeight="Bold" Background="#8fbc8f"/>
                </StackPanel>

            </StackPanel>

            <StackPanel Orientation="Vertical" HorizontalAlignment="Right">
                <Button Content="画像保存" x:Name="ImageButton" Width="200" Height="40" Margin="10,10,5,5" FontWeight="Bold"/>
            </StackPanel>

        </StackPanel>

        <ListView x:Name="PersonListView" Grid.Row="1" Margin="0,10,0,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="氏名" Width="180" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="電話番号" Width="180" DisplayMemberBinding="{Binding Phone}"/>
                    <GridViewColumn Header="住所" Width="180" DisplayMemberBinding="{Binding Address}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>